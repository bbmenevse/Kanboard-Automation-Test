package tests.isolatedtests.userdetailstests;

/**
 * I realised that the edit profile on user details use the same controller as the one
 * in user managements. So there is no point in doing this test.
 */
public class EditProfileTest {
    
}
